version: '3'
services:
  react-app:
    container_name: react-app
    build:
      context: .
      dockerfile: ./react-app/Dockerfile
    restart: always
    networks:
      - mack-check-Network
    ports:
      - "80:80"

networks:
  mack-check-Network:
    driver: bridge      
